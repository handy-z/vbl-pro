{
  "name": "vbl-pro",
  "module": "src/main.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/main.ts",
    "dev": "bun run --watch src/main.ts",
    "check": "bunx tsc --noEmit",
    "build": "bun run build:overlay && bun run clean:build && bun build src/main.ts src/workers/pixel.ts src/workers/x2.ts src/workers/focus.ts --target=bun --minify --outdir ./build",
    "compile": "bun run build:overlay && bun run clean:release && bun build src/main.ts src/workers/pixel.ts src/workers/x2.ts src/workers/focus.ts --compile --target=bun --bytecode --minify --sourcemap --outfile ./release/vbl-pro.exe",
    "build:overlay": "napi build --manifest-path native/overlay/Cargo.toml --package-json-path native/overlay/package.json --release --output-dir native/overlay --no-dts-header",
    "clean:build": "bun -e \"require('node:fs').rmSync('./build',{recursive:true,force:true})\"",
    "clean:release": "bun -e \"require('node:fs').rmSync('./release',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "@napi-rs/cli": "latest",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "keysender": "latest",
    "uiohook-napi": "latest"
  },
  "trustedDependencies": [
    "keysender",
    "uiohook-napi"
  ]
}
